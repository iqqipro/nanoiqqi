x-common-config: &common-config
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - ~/.nanoiqqi:/root/.nanoiqqi

services:
  nanoiqqi-gateway:
    container_name: nanoiqqi-gateway
    <<: *common-config
    command: ["gateway"]
    restart: unless-stopped
    environment:
      - NANOIQQI_BRAIN_OFFICE__URL=http://brain-office:8765
    ports:
      - 18790:18790
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M
  
  nanoiqqi-cli:
    <<: *common-config
    profiles:
      - cli
    command: ["status"]
    stdin_open: true
    tty: true
    environment:
      - NANOIQQI_BRAIN_OFFICE__URL=http://brain-office:8765

  brain-office:
    build:
      context: ./brain-office
      dockerfile: Dockerfile
    container_name: brain-office
    ports:
      - "8765:8765"
    restart: unless-stopped
